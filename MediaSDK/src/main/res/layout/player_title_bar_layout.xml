<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="@dimen/player_title_height">

    <ImageView
        android:id="@+id/back"
        android:layout_width="@dimen/player_title_height"
        android:layout_height="@dimen/player_title_height"
        android:layout_gravity="center"
        android:padding="@dimen/player_title_icon_padding"
        android:src="@drawable/dkplayer_ic_action_arrow_back" />

    <TextView
        android:id="@+id/title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:ellipsize="marquee"
        android:marqueeRepeatLimit="marquee_forever"
        android:paddingLeft="6dp"
        android:paddingRight="6dp"
        android:singleLine="true"
        android:textColor="@android:color/white"
        android:textSize="@dimen/player_title_text_size"
        tools:text="这是一个标题" />

    <ImageView
        android:id="@+id/battery"
        android:layout_width="26dp"
        android:layout_height="20dp"
        android:src="@drawable/dkplayer_battery_level" />

    <TextView
        android:id="@+id/system_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="14dp"
        android:layout_marginRight="14dp"
        android:textColor="@android:color/white"
        android:textSize="@dimen/player_title_system_text_size"
        tools:text="12:00" />
</merge>